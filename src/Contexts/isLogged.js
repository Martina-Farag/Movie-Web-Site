import React from "react";

export const isLoggedContext = React.createContext();

export const IsloggedProvider = isLoggedContext.Provider;   // Provider_name hase to start with upper case letters